{
	"dependencies": {
		"arpscan": "^1.0.2",
		"cors": "^2.8.5",
		"express": "^4.22.1",
		"network": "^0.6.1"
	},
	"description": "A simple API to get the IP address of the device on the local network.",
	"devDependencies": {
		"concurrently": "^8.2.2",
		"nodemon": "^1.17.1"
	},
	"main": "index.js",
	"name": "local-ip-api",
	"scripts": {
		"start": "node server.js && node app.js",
		"dev": "concurrently \"nodemon server.js\" \"nodemon app.js\""
	},
	"version": "1.0.0"
}
